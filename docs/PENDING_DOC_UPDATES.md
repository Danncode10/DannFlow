# Pending Documentation Updates

- **[docs/dannflow_docs/workflows/branching-and-sync.md]** or **[architecture]**: The `.husky/commit-msg` hook was upgraded to an inverted logic model. It now tracks _all_ changes across the entire repo (except `docs/`, `scripts/`, `public/`, `README.md`, etc.) to enforce strict documentation governance. This should be added to the internal DannFlow documentation.
